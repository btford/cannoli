h2 new the page
form(ng-submit='createPage()')
  h3 title
  input(ng-model='page.title')
  h3 body
  textarea(ng-model='page.content')
  h4 cool
  input(type='submit', value='Donesies')

  hr

  h4 preview: {{page.title}}
  div(btf-markdown='page.content')
